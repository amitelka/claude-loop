# Repo notes

This repo is PUBLIC. Before any commit: sweep for personal names and preference-referencing wording — `per <name>`, possessive references, named users in comments/tests/config/.gitignore/example paths — not just secrets/PII (standard credential scanners pass cleanly on this class). Attribute behavior/config choices to decision records or generic "operator" language. Git author identity is a different thing and stays.
